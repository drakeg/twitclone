name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - agent/ci-runner-diagnostic

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI smoke validation succeeded"
