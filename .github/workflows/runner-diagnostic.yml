name: Actions runner diagnostic

on:
  push:
    branches:
      - agent/ci-runner-diagnostic
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runner-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Confirm runner starts
        run: echo "GitHub-hosted runner started successfully"
