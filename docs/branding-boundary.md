# Branding boundary

Ripple is the public-facing product name. The `twitclone` repository and Python
package names remain implementation identifiers until a separate namespace
migration is justified.
