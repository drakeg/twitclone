# Internal naming after the Ripple rebrand

`twitclone` remains the implementation namespace for Python imports, repository
URLs, and established local-development commands. It is not the public product
name. User-facing copy should use Ripple.
